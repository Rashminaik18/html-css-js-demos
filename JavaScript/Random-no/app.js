// Generating random number between 1 to 100
let num=Math.floor(Math.random()*100)+1;
console.log(`Random number generation between 1 and 100: ${num}`);

//Generating random number between 1 to 5
let x=Math.floor(((Math.random()*10)+1)/2);
console.log(`Random number genration between 1 and 5 : ${x}`);

//Generating random number between 1 and 6
let y=Math.floor(Math.random()*6)+1
console.log("Random number between 1 and 6:",y);